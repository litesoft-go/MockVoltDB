docker run --rm --network=host alpine ash -c "apk add socat && socat TCP-LISTEN:5000,reuseaddr,fork TCP:$(minikube ip):5000"
if [[ $? -ne 0 ]]; then
    echo docker port forwarding Failed
    exit 1
fi
